# admin-web 다국어(i18n) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** admin-web에 한국어/영어/일본어 런타임 전환 i18n 시스템을 구축하고 하드코딩 문자열을 전량 마이그레이션한다.

**Architecture:** 루트 `/i18n`을 `client/`(기존, 빌드타임 kr/jp)와 `admin/`(신규, 런타임 ko/en/ja) 서브디렉토리로 재편하고 DotPath 타입 코어를 공유한다. admin-web은 React Context 기반 `LanguageProvider` + `useT()` 훅으로 런타임 전환하며, 선택 언어는 localStorage에 저장한다.

**Tech Stack:** TypeScript, React 18 Context (라이브러리 추가 없음), tsx 스크립트 검증

**Spec:** `docs/superpowers/specs/2026-08-17-admin-web-i18n-design.md`

## Global Constraints

- 커밋 메시지는 한글. `git add`는 항상 명시 경로 (`-A` 금지).
- 변수/파라미터 약어 금지 (`e`, `req` 등 → 풀네임).
- 중첩 삼항연산자 금지.
- client-web의 기존 import(`@i18n`)와 동작은 변경하지 않는다 — `i18n/index.ts`가 client를 재수출.
- admin 메시지 리프는 `{ ko: string; en: string; ja: string }` — 세 언어 모두 필수.
- 한·영·일 어순 차이 때문에 문자열 조립(concat) 금지 — 변수 포함 문장은 `{param}` 보간 사용.
- 마이그레이션 제외: API 서버 응답 메시지, DB 데이터 값, 로그·주석.
- 각 태스크 검증: `pnpm --filter @jsure/admin-web typecheck` + `npx tsx i18n/scripts/validate-i18n.ts` 통과.

---

### Task 1: i18n 루트 재편 — core / client / admin

**Files:**
- Create: `i18n/core.ts`
- Create: `i18n/client/messages.ts` (기존 `i18n/messages.ts` git mv)
- Create: `i18n/client/t.ts` (기존 `i18n/t.ts` git mv)
- Create: `i18n/admin/messages.ts`
- Create: `i18n/admin/index.ts`
- Modify: `i18n/index.ts`
- Modify: `i18n/scripts/validate-i18n.ts`
- Test: `i18n/scripts/test-admin-translate.ts`

**Interfaces:**
- Consumes: 없음 (최초 태스크)
- Produces:
  - `i18n/core.ts` → `type DotPath<T, Leaf>` (제네릭 dot-path 타입)
  - `i18n/admin/index.ts` → `type AdminLanguage = "ko" | "en" | "ja"`, `type AdminTranslationKey`, `function translate(key: AdminTranslationKey, language: AdminLanguage, params?: Record<string, string | number>): string`, `const adminMessages`
  - `i18n/index.ts` → 기존과 동일하게 `t`, `TranslationKey` 재수출 (client-web 무변경)

- [ ] **Step 1: 기존 파일 이동**

```bash
mkdir -p i18n/client i18n/admin
git mv i18n/messages.ts i18n/client/messages.ts
git mv i18n/t.ts i18n/client/t.ts
```

- [ ] **Step 2: 공유 코어 작성**

`i18n/core.ts`:

```ts
export type DotPath<T, Leaf> = T extends Leaf
  ? ""
  : T extends object
    ? {
        [K in keyof T & string]: DotPath<T[K], Leaf> extends "" ? K : `${K}.${DotPath<T[K], Leaf>}`;
      }[keyof T & string]
    : never;

export function resolveLeaf(root: unknown, key: string): Record<string, unknown> {
  const segments = key.split(".");
  let node: unknown = root;
  for (const segment of segments) {
    if (node && typeof node === "object" && segment in node) {
      node = (node as Record<string, unknown>)[segment];
      continue;
    }
    throw new Error(`[i18n] Unknown key: ${key}`);
  }
  if (!node || typeof node !== "object") {
    throw new Error(`[i18n] Key is not a leaf: ${key}`);
  }
  return node as Record<string, unknown>;
}
```

- [ ] **Step 3: client/t.ts를 코어 사용으로 정리**

`i18n/client/t.ts` 전체 교체 (동작 동일, lookup만 코어 위임):

```ts
import { messages } from "./messages";
import { resolveLeaf, type DotPath } from "../core";

type Region = "kr" | "jp";
const raw = import.meta.env.VITE_I18N_REGION;
const region: Region = raw === "jp" ? "jp" : "kr";

type Leaf = { readonly kr: string; readonly jp: string };

export type TranslationKey = DotPath<typeof messages, Leaf>;

export function t(key: TranslationKey): string {
  const leaf = resolveLeaf(messages, key);
  const value = leaf[region];
  if (typeof value === "string") return value;
  throw new Error(`[i18n] Missing "${region}" for key: ${key}`);
}
```

- [ ] **Step 4: admin 모듈 작성**

`i18n/admin/messages.ts` (시드 — 마이그레이션 태스크에서 계속 확장):

```ts
export const adminMessages = {
  common: {
    languageName: { ko: "한국어", en: "English", ja: "日本語" },
  },
} as const;
```

`i18n/admin/index.ts`:

```ts
import { adminMessages } from "./messages";
import { resolveLeaf, type DotPath } from "../core";

export { adminMessages };

export type AdminLanguage = "ko" | "en" | "ja";
export const ADMIN_LANGUAGES: readonly AdminLanguage[] = ["ko", "en", "ja"];

type AdminLeaf = { readonly ko: string; readonly en: string; readonly ja: string };

export type AdminTranslationKey = DotPath<typeof adminMessages, AdminLeaf>;

export function translate(
  key: AdminTranslationKey,
  language: AdminLanguage,
  params?: Record<string, string | number>,
): string {
  const leaf = resolveLeaf(adminMessages, key);
  const template = leaf[language];
  if (typeof template !== "string") {
    throw new Error(`[i18n] Missing "${language}" for key: ${key}`);
  }
  if (!params) return template;
  return template.replace(/\{(\w+)\}/g, (match, name: string) => {
    if (name in params) return String(params[name]);
    return match;
  });
}
```

- [ ] **Step 5: index.ts 재수출 유지**

`i18n/index.ts`:

```ts
export { t } from "./client/t";
export type { TranslationKey } from "./client/t";
```

- [ ] **Step 6: validate 스크립트 일반화**

`i18n/scripts/validate-i18n.ts` 전체 교체:

```ts
import { messages } from "../client/messages";
import { adminMessages } from "../admin/messages";

const missing: string[] = [];

function walk(node: unknown, path: string[], locales: readonly string[]): void {
  if (node === null || typeof node !== "object") {
    missing.push(`${path.join(".")}: leaf가 객체가 아님`);
    return;
  }
  const record = node as Record<string, unknown>;
  const isLeaf = locales.some((locale) => locale in record);
  if (isLeaf) {
    for (const locale of locales) {
      const value = record[locale];
      if (typeof value !== "string" || value.trim() === "") {
        missing.push(`${path.join(".")}.${locale}: 값이 비어있음`);
      }
    }
    return;
  }
  for (const key of Object.keys(record)) {
    walk(record[key], [...path, key], locales);
  }
}

walk(messages, ["client"], ["kr", "jp"]);
walk(adminMessages, ["admin"], ["ko", "en", "ja"]);

if (missing.length > 0) {
  console.error("[i18n] 번역 누락:");
  for (const item of missing) console.error(`  - ${item}`);
  console.error(`\n총 ${missing.length}건.`);
  process.exit(1);
}

console.log("[i18n] 모든 leaf 검증 통과");
```

- [ ] **Step 7: translate 셀프체크 테스트 작성 후 실행**

`i18n/scripts/test-admin-translate.ts`:

```ts
import assert from "node:assert";
import { translate } from "../admin/index";

assert.strictEqual(translate("common.languageName", "ko"), "한국어");
assert.strictEqual(translate("common.languageName", "ja"), "日本語");

// 보간: {name} 치환, 없는 파라미터는 원문 유지
// (시드 메시지에 보간 리프가 없으므로 정규식 동작만 별도 확인)
const replaced = "총 {count}건".replace(/\{(\w+)\}/g, (match, name: string) =>
  name === "count" ? "3" : match,
);
assert.strictEqual(replaced, "총 3건");

assert.throws(() => translate("common.languageName", "xx" as never));

console.log("[i18n] translate 셀프체크 통과");
```

실행: `npx tsx i18n/scripts/test-admin-translate.ts`
기대: `[i18n] translate 셀프체크 통과`

- [ ] **Step 8: 검증**

```bash
npx tsx i18n/scripts/validate-i18n.ts
pnpm --filter @jsure/client-web typecheck
```

기대: 둘 다 통과 (client-web은 import 경로 무변경이므로 그대로 통과해야 함).

- [ ] **Step 9: 커밋**

```bash
git add i18n/core.ts i18n/client/messages.ts i18n/client/t.ts i18n/admin/messages.ts i18n/admin/index.ts i18n/index.ts i18n/scripts/validate-i18n.ts i18n/scripts/test-admin-translate.ts
git commit -m "refactor(i18n): client/admin 서브디렉토리로 재편, admin 3개 언어 코어 추가"
```

---

### Task 2: admin-web 런타임 레이어 + alias + 빌드 연결

**Files:**
- Create: `apps/admin-web/src/lib/i18n.tsx`
- Modify: `apps/admin-web/vite.config.ts` (alias 추가)
- Modify: `apps/admin-web/tsconfig.app.json` (paths + include 추가)
- Modify: `apps/admin-web/package.json` (build 스크립트에 validate)
- Modify: `apps/admin-web/src/App.tsx` (Provider 래핑)

**Interfaces:**
- Consumes: Task 1의 `translate`, `AdminLanguage`, `AdminTranslationKey`, `ADMIN_LANGUAGES` (`@i18n/admin`)
- Produces:
  - `useT(): (key: AdminTranslationKey, params?: Record<string, string | number>) => string`
  - `useLanguage(): { language: AdminLanguage; setLanguage: (language: AdminLanguage) => void }`
  - `getStoredLanguage(): AdminLanguage` — React 밖(axios 등)에서 사용
  - `LanguageProvider` — App 루트에 장착

- [ ] **Step 1: alias/paths 추가**

`apps/admin-web/vite.config.ts`의 `resolve.alias`에 추가:

```ts
    alias: {
      "@": path.resolve(__dirname, "./src"),
      "@i18n": path.resolve(__dirname, "../../i18n"),
    },
```

`apps/admin-web/tsconfig.app.json`:

```json
    "paths": {
      "@/*": ["src/*"],
      "@i18n/*": ["../../i18n/*"]
    }
```

`include`를 `["src", "../../i18n/**/*.ts"]`로 변경.

- [ ] **Step 2: 런타임 레이어 작성**

`apps/admin-web/src/lib/i18n.tsx`:

```tsx
import { createContext, useCallback, useContext, useMemo, useState } from "react";
import type { ReactNode } from "react";
import {
  ADMIN_LANGUAGES,
  translate,
  type AdminLanguage,
  type AdminTranslationKey,
} from "@i18n/admin";

const STORAGE_KEY = "admin-language";

export function getStoredLanguage(): AdminLanguage {
  const stored = localStorage.getItem(STORAGE_KEY);
  if (stored && (ADMIN_LANGUAGES as readonly string[]).includes(stored)) {
    return stored as AdminLanguage;
  }
  return "ko";
}

type LanguageContextValue = {
  language: AdminLanguage;
  setLanguage: (language: AdminLanguage) => void;
};

const LanguageContext = createContext<LanguageContextValue | null>(null);

export function LanguageProvider({ children }: { children: ReactNode }) {
  const [language, setLanguageState] = useState<AdminLanguage>(getStoredLanguage);

  const setLanguage = useCallback((next: AdminLanguage) => {
    localStorage.setItem(STORAGE_KEY, next);
    setLanguageState(next);
  }, []);

  const value = useMemo(() => ({ language, setLanguage }), [language, setLanguage]);

  return <LanguageContext.Provider value={value}>{children}</LanguageContext.Provider>;
}

export function useLanguage(): LanguageContextValue {
  const context = useContext(LanguageContext);
  if (!context) throw new Error("useLanguage must be used within LanguageProvider");
  return context;
}

export function useT() {
  const { language } = useLanguage();
  return useCallback(
    (key: AdminTranslationKey, params?: Record<string, string | number>) =>
      translate(key, language, params),
    [language],
  );
}
```

- [ ] **Step 3: App 루트에 Provider 장착**

`apps/admin-web/src/App.tsx`에서 최상위 요소를 `LanguageProvider`로 감싼다 (기존 Provider들 바깥 또는 바로 안쪽, 라우터보다 위):

```tsx
import { LanguageProvider } from "@/lib/i18n";
// 기존 return (...) 을
return <LanguageProvider>{/* 기존 트리 */}</LanguageProvider>;
```

- [ ] **Step 4: build에 validate 연결**

`apps/admin-web/package.json`:

```json
    "build": "tsx ../../i18n/scripts/validate-i18n.ts && tsc -b && vite build",
```

devDependencies에 `tsx`가 admin-web에서 resolve되는지 확인 — client-web과 동일하게 루트 또는 워크스페이스에 이미 있으면 추가 불필요. 없으면 client-web의 package.json과 같은 방식을 그대로 따른다.

- [ ] **Step 5: 검증**

```bash
pnpm --filter @jsure/admin-web typecheck
```

기대: 통과.

- [ ] **Step 6: 커밋**

```bash
git add apps/admin-web/src/lib/i18n.tsx apps/admin-web/vite.config.ts apps/admin-web/tsconfig.app.json apps/admin-web/package.json apps/admin-web/src/App.tsx
git commit -m "feat(admin-web): 런타임 언어 전환 레이어(LanguageProvider·useT) 추가"
```

---

### Task 3: 헤더 언어 전환 드롭다운

**Files:**
- Modify: `apps/admin-web/src/components/Header/` 내 헤더 컴포넌트 (실제 파일명은 디렉토리 확인 후)
- Modify: `i18n/admin/messages.ts` (필요 키 추가)

**Interfaces:**
- Consumes: Task 2의 `useLanguage`, `ADMIN_LANGUAGES`
- Produces: 헤더에 언어 셀렉트 UI (다른 태스크가 의존하지 않음)

- [ ] **Step 1: 헤더에 셀렉트 추가**

기존 헤더 스타일 관례를 따라 네이티브 `<select>` 사용 (신규 드롭다운 컴포넌트 만들지 않음):

```tsx
import { useLanguage } from "@/lib/i18n";
import { ADMIN_LANGUAGES, type AdminLanguage } from "@i18n/admin";

const LANGUAGE_LABELS: Record<AdminLanguage, string> = {
  ko: "한국어",
  en: "English",
  ja: "日本語",
};

// 헤더 JSX 내부:
<select
  value={language}
  onChange={(event) => setLanguage(event.target.value as AdminLanguage)}
  aria-label="Language"
>
  {ADMIN_LANGUAGES.map((languageOption) => (
    <option key={languageOption} value={languageOption}>
      {LANGUAGE_LABELS[languageOption]}
    </option>
  ))}
</select>
```

라벨은 각 언어의 자기 표기이므로 번역 대상이 아니다 (고정 표기).

- [ ] **Step 2: 검증**

```bash
pnpm --filter @jsure/admin-web typecheck
```

- [ ] **Step 3: 커밋**

```bash
git add apps/admin-web/src/components/Header i18n/admin/messages.ts
git commit -m "feat(admin-web): 헤더에 언어 전환 드롭다운 추가"
```

---

### Task 4~13: 문자열 마이그레이션 (영역별)

각 태스크는 동일한 절차를 따른다. **마이그레이션 공통 절차** (모든 태스크에 적용):

1. 대상 디렉토리에서 한글 문자열 위치 파악:
   `grep -rn '[가-힣]' <대상 경로> --include='*.tsx' --include='*.ts'`
2. 각 문자열을 `i18n/admin/messages.ts`에 dot-path 키로 추가 — ko는 원문, en/ja는 즉시 번역. 키는 `<영역>.<컴포넌트>.<의미>` 형식. 예:

```ts
// messages.ts에 추가
pages: {
  applicants: {
    title: { ko: "지원자 관리", en: "Applicant Management", ja: "応募者管理" },
    searchPlaceholder: {
      ko: "이름, 이메일 검색",
      en: "Search by name or email",
      ja: "名前・メールで検索",
    },
    totalCount: { ko: "총 {count}건", en: "{count} total", ja: "全{count}件" },
  },
},
```

3. 컴포넌트에서 교체:

```tsx
// before
<h1>지원자 관리</h1>
<span>총 {total}건</span>

// after
import { useT } from "@/lib/i18n";
const t = useT();
<h1>{t("pages.applicants.title")}</h1>
<span>{t("pages.applicants.totalCount", { count: total })}</span>
```

4. 컴포넌트 밖 상수(enum 라벨 맵, zod 메시지 등)는 두 가지 중 하나:
   - 라벨 맵: 값을 키로 바꾸고 사용처에서 `t(map[status])` — 맵 타입은 `Record<Status, AdminTranslationKey>`
   - 렌더 시점에 언어가 필요한 유틸 함수: `t`를 인자로 받도록 시그니처 변경
5. 제외: API 응답 메시지 표시부(서버 문자열 그대로 출력하는 곳), 로그, 주석, DB 데이터 값.
6. 검증:

```bash
npx tsx i18n/scripts/validate-i18n.ts
pnpm --filter @jsure/admin-web typecheck
grep -rn '[가-힣]' <대상 경로> --include='*.tsx' --include='*.ts' | grep -v '^\s*//' 
```

grep 잔여는 주석·로그·제외 대상만 남아야 한다.

7. 커밋 (대상 경로 + `i18n/admin/messages.ts` 명시 add):

```bash
git add <대상 경로의 수정 파일들> i18n/admin/messages.ts
git commit -m "feat(admin-web): <영역> 문자열 i18n 마이그레이션"
```

**태스크 분할** (각각 위 절차 수행, 독립 커밋):

- [ ] **Task 4:** `layouts/` + `components/Sidebar` + `components/Header` + `components/RequireAuth.tsx` — 내비게이션 메뉴명, 로그아웃 등
- [ ] **Task 5:** `components/ui` (11개) — 버튼, 모달, 페이지네이션 등 공통 위젯
- [ ] **Task 6:** `components/composites` (14개) — 데이터테이블, 필터바 등
- [ ] **Task 7:** `domains/auth` + `pages/Login` + `pages/Register` + `pages/NotFound`
- [ ] **Task 8:** `domains/application` 1/2 — `applicants` 하위 (필터, 테이블, 다이얼로그)
- [ ] **Task 9:** `domains/application` 2/2 — 나머지 + `pages/Applicants` + `pages/Drafts`
- [ ] **Task 10:** `domains/campaign` + `pages/Campaigns` + `pages/Brands`
- [ ] **Task 11:** `domains/notice` + `pages/Notices` + `domains/broadcast` + `domains/messageTemplate` + `pages/MessageTemplates`
- [ ] **Task 12:** `domains/influencer` + `pages/Influencers` + `domains/team` + `pages/Team` + `domains/overview` + `pages/Overview`
- [ ] **Task 13:** `domains/report` + `pages/Reports` + `pages/Payouts` + `lib/` 잔여 문자열

**주의 (Task 8·9):** 현재 워크트리 기준(main)으로 작업한다. 사용자 체크아웃에 `ApplicantFilters.tsx` 등 미커밋 변경이 있으므로, 완료 후 main 병합 시 충돌 가능성을 최종 보고에 명시할 것.

---

### Task 14: 최종 검증

**Files:**
- 수정 없음 (검증 전용). 잔여 발견 시 해당 파일 + `i18n/admin/messages.ts` 수정.

**Interfaces:**
- Consumes: Task 1~13 전체
- Produces: 완료 판정

- [ ] **Step 1: 전체 한글 리터럴 스캔**

```bash
grep -rn '[가-힣]' apps/admin-web/src --include='*.tsx' --include='*.ts' | grep -v '^\s*//' | grep -v '//.*[가-힣]'
```

잔여가 주석·로그·제외 대상뿐인지 눈으로 확인. UI 문자열이 남았으면 해당 영역 절차로 처리 후 커밋.

- [ ] **Step 2: 전체 빌드 검증**

```bash
npx tsx i18n/scripts/validate-i18n.ts
npx tsx i18n/scripts/test-admin-translate.ts
pnpm --filter @jsure/admin-web typecheck
pnpm --filter @jsure/admin-web build
pnpm --filter @jsure/client-web build
```

기대: 전부 통과. client-web 빌드 통과는 재편이 무해했음의 증명.

- [ ] **Step 3: 커밋 & 푸시**

잔여 수정이 있었으면 커밋. 브랜치 push.
