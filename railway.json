{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm turbo build --filter=@jsure/api && pnpm --filter @jsure/api exec prisma generate"
  },
  "deploy": {
    "startCommand": "pnpm --filter @jsure/api exec prisma migrate deploy && pnpm --filter @jsure/api start:prod",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE"
  }
}
